{
  "version": 6,
  "cmakeMinimumRequired": {
    "major": 3,
    "minor": 24,
    "patch": 0
  },
  "configurePresets": [
    {
      "name": "dev",
      "displayName": "{{PROJECT_NAME}} development",
      "binaryDir": "${sourceDir}/build",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "{{BUILD_TYPE}}",
        "CMAKE_PREFIX_PATH": "${sourceDir}/.kraken/sdk"
      }
    }
  ],
  "buildPresets": [
    {
      "name": "dev",
      "configurePreset": "dev",
      "configuration": "{{BUILD_TYPE}}"
    }
  ]
}
